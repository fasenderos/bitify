{
  "name": "bitify-root",
  "version": "0.0.0",
  "private": true,
  "description": "Ultra-fast Open Source Trading Platform for NodeJS",
  "license": "Apache-2.0",
  "scripts": {
    "ci:publish": "pnpm publish -r",
    "cleanall": "rm -rf pnpm-lock.yaml node_modules */**/node_modules && pnpm workspace -- clean",
    "clean": "pnpm workspace -- clean",
    "format": "pnpm workspace -- format",
    "format:fix": "pnpm workspace -- format:fix",
    "lint": "pnpm workspace -- lint",
    "lint:fix": "pnpm workspace -- lint:fix",
    "prepare": "husky install",
    "release": "pnpm workspace -- release",
    "test": "pnpm workspace -- test",
    "test:dev": "pnpm workspace -- test:dev",
    "start": "pnpm workspace -- start",
    "start:dev": "pnpm workspace -- start:dev",
    "workspace": "pnpm -r --workspace-concurrency=1"
  },
  "packageManager": "pnpm@8.6.10",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@tsconfig/node-lts": "^18.12.3",
    "@tsconfig/strictest": "^2.0.1",
    "husky": "^8.0.0",
    "lint-staged": "^14.0.1"
  },
  "keywords": [
    "trading",
    "cryptocurrency",
    "exchange",
    "trading-platform",
    "matching-engine",
    "bitify",
    "blockchain",
    "crypto-exchange-software",
    "cryptocurrency-exchange-software"
  ]
}
